{% extends 'base.html.twig' %}

{% block title %}<h1>Suppression du fournisseur</h1>{% endblock %}

{% block body %}
    {{ form_start(form) }}
    <div class="container">
        <h2>{{ fournisseur.name }}</h2>
        {# <p>Catégorie : {{ fournisseur.category.name }}</p>#}
        <p> Voulez-vous supprimer ce fournisseur ? </p>

        {{ form_widget(form.delete) }}

        <a href="{{ path ('fournisseur_details', {'id': fournisseur.id}) }}">
            <input class="btn btn-success"
                   type="button"
                   value="Annuler">
        </a>

        {{ form_end(form) }}
    </div>
{% endblock %}